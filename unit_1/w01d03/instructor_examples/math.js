// camel case for var names
let carTotal = 16.07
// Math is the global object
let roundedUpCart = Math.ceil(carTotal)
let donation = (roundedUpCart - carTotal).toFixed(2)
//console.log(donation)
let rand = Math.random() * 25
console.log(Math.ceil(rand))
