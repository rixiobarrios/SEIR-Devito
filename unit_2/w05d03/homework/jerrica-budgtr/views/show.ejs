<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>BUDGTR</title>
    <!-- ========= SKELETON & NORMALIZE CSS ========= -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/normalize/8.0.0/normalize.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/skeleton/2.0.4/skeleton.css">
  </head>
  <body>
    <div class="container">
      <h1><%=budget.name%></h1>
      <!-- etc. budget data in a dl (description list)-->
      <dl>
        <!-- loop through the budget's data object and create dt/dd for each one -->
        <% for(let budgetInfo in budget) { %>
          <dt><strong><%=budgetInfo%></strong></dt>
          <dd><%=budget[budgetInfo]%></dd>
        <% } %>
      </dl>
      <!-- button to go back -->
      <a href="/budgets"><button>back</button></a>
    </div>
  </body>
</html>
